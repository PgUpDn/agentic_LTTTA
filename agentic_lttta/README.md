# agentic_lttta

Python package for the Agentic LTTTA baseline prototype.

See the repository-level `README.md` for setup, data layout, competition notes,
and run commands. The online controller is bounded and does not call an LLM in
the scored loop; the optional Google ADK layer is only for local offline policy
design.
